<h2>Current Forecast in Area</h2>


<%= form_with(method: 'get', local:true) do %>
    <%= label :address, "Please entre given address to search"%>
    <%= text_field_tag(:address, @address || 'Currently no entry') %>
    <%= submit_tag("Let's go!") %>
<% end %>

<p><%= Date.today.strftime("%A, %B %d") %><p>
<img class="mx-auto" src="http://openweathermap.org/img/wn<%= @data["weather"].first["icon"] %>@2x.png"><img>

<% unless defined?(@weather) == false %>
    <ul>
        <li>Current Temperature: <%= @weather.temperture %></li>
        <li>Low: <%= @wather.temprature_min %></li>
        <li>High: <%= @weather.temperature_max %></li>
        <li>Humidity: <%= @weather.humidity %></li>
        <li>Description: <%= @weather.description %></li>
        <li> Cache Result? <%= @cache_present%>

